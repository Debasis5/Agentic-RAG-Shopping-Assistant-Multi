from src.supervisor.state import SupervisorState

# Backwards-compatible alias — api.py and main.py import GraphState from here.
# Will be updated to import SupervisorState directly in Step 8.
GraphState = SupervisorState
